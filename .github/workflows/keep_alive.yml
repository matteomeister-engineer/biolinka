name: Keep Biolinka Awake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Biolinka Streamlit app
        run: |
          curl -sS -L -o /dev/null -w "HTTP=%{http_code}\n" https://biolinka.streamlit.app/